# {{{ Include files
includes:
  - app:
    - name: operating-system
# -------------------------------------------------------------------------- }}}
# {{{ Installations
installations:
  - os:
    - name: linux
      command:
       - sudo: true
         program: apt-get 
         argument: install -y openjdk-8-jdk

# -------------------------------------------------------------------------- }}}

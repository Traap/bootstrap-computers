# {{{ Installations
installations:
  - os:
    - name: linux
      command:
       - sudo: true
         program: wget
         argument: -N http://selenium-release.storage.googleapis.com/selenium-server-standalone-3.4.0.jar -P ~/

       - sudo: true
         program: mv 
         argument: mv -f ~/selenium-server-standalone-3.4.0.jar /usr/local/bin/selenium-server-standalone.jar

       - sudo: true
         program: chown 
         argument: root:root /usr/local/bin/selenium-server-standalone.jar

       - sudo: true
         program: chmod 
         argument: 0755 /usr/local/bin/selenium-server-standalone.jar
# -------------------------------------------------------------------------- }}}

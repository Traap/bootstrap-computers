# {{{ Repositories
repos:
  - repo:
    url: http://github.com
    paths:
      - source: Traap/docbld
        target: ~/git/docbld

# -------------------------------------------------------------------------- }}}

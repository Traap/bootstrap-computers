# {{{ Include files
includes:
  - app:
    - name: operating-system
# -------------------------------------------------------------------------- }}}
# {{{ Installations
installations:
  - os:
   - name: linux
     command:
      - sudo: true
        program: apt-get
        argument: install -y eclipse-platform

   - name: cygwin
     command:
      - sudo: false
        program: choco
        argument: install -y eclipse 

   - name: mingw32
     command:
      - sudo: false
        program: choco
        argument: install -y eclipse 

# -------------------------------------------------------------------------- }}}

# {{{ Installations
installations:
  - os:
    - name: mingw32 
      command:
       - sudo: false
         program: choco install -y
         argument: atom 
# ------------------------------------------------------------------------- }}}

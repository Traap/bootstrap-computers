# {{{ Installations
installations:
  - os:
    - name: mingw32 
      command:
       - sudo: false
         program: choco 
         argument: install atom -y 
# ------------------------------------------------------------------------- }}}

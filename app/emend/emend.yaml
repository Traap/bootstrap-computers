# {{{ Repositories
repos:
  - repo:
    url: http://github.com
    paths:
      - source: Traap/emend
        target: ~/git/emend

# -------------------------------------------------------------------------- }}}

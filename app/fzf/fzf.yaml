# {{{ Installations
installations:
 - os:
   - name: mingw32 
     command:
      - sudo: false
        program: choco install -y 
        argument: fzf 

   - name: linux-gnu 
     command:
      - sudo: no 
        program: pacman -Sy --noconfirm  
        argument: fzf 
# -------------------------------------------------------------------------- }}}

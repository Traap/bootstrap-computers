# {{{ Installations
installations:
  - os:
    - name: cygwin 
      command:
       - sudo: false 
         program: choco
         argument: install -y ie11

    - name: mingw32
      command:
       - sudo: false 
         program: choco
         argument: install -y ie11 
# -------------------------------------------------------------------------- }}}

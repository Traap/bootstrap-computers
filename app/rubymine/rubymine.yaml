# {{{ Installations
installations:
 - os:
   - name: cygwin 
     command:
      - sudo: false
        program: choco
        argument: install -y rubymine

   - name: mingw32
     command:
      - sudo: false
        program: choco
        argument: install -y rubymine
# -------------------------------------------------------------------------- }}}

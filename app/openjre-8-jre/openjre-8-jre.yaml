# {{{ Include files
includes:
  - app:
    - name: operating-system

# -------------------------------------------------------------------------- }}}
# {{{ Installations
installations:
  - os:
    - name: linux
      command:
       - sudo: true
         program: apt-get install -y
         argument: openjre-8-jre

    - name: cygwin 
      command:
       - sudo: false 
         program: choco install -y
         argument: jre8 

# -------------------------------------------------------------------------- }}}

# {{{ Installations
installations:
  - os:
    - name: darwin
      command:
       - sudo: false
         program: brew install
         argument: openconnect 
 
# -------------------------------------------------------------------------- }}}

# {{{ Installations
installations:
  - os:
    - name: darwin
      command:
       - sudo: false
         program: brew 
         argument: install openconnect 
 
# -------------------------------------------------------------------------- }}}

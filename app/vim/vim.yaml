# {{{ Repositories 

repos:
  - repo:
    url: http://github.com
    paths:
      - source: Traap/vim
        target: ~/git/vim

# -------------------------------------------------------------------------- }}}
# {{{ Installations

installations:
 - os:
   # Will experiment running Vim from a subshell.  This may fail misserably. 
   - name: any
     command:
      - sudo: false
        program: (vim +qall)

# -------------------------------------------------------------------------- }}}

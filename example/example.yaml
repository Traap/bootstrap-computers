# {{{ Include files
includes:
  - bundle:
    - name: basic
  - file:
    - name: app/dotfiles/dotfiles.yaml
  - app:
    - name: vim 
# -------------------------------------------------------------------------- }}}
